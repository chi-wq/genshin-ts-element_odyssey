// === 游戏规则文本 ===
// 使用模板字面量书写，预处理将换行转为 \\n
// str() 为 gsts 全局函数，由 installServerGlobals() 提供

export const RuleTitle = str('游戏规则')

const rawRuleBody = `
【目标】全灭敌人并集齐所需元素球即可通关。

【元素球】普通球(冰/火/水/雷)带对应图标，触碰更新主元素(左上方)，原主元素变为副元素(右上方)，+30分。
击杀敌人后场上所有球5秒内可拾取，超时变为深渊球。特殊球也会超时变为深渊球。

【特殊球/特殊元素球】独特造型(风环/岩盾/草色十字/光柱)，不计入通关球数：
风环→+30秒　岩盾→护盾(20秒)　草色十字→回复10000生命　光柱→全灭敌人

【深渊球】散发紫色邪气，不可拾取。触碰→对我方发起追踪攻击，大量伤害。
击杀敌人或用净化道具可恢复为可拾取(5秒内)。

【元素反应】持有主+副元素时攻击触发(融化/冻结/超导/蒸发/超载/感电)，副元素仅附着，主元素全额伤害。
触发期间击杀敌人额外+100分。

【计分】普通击杀+1分，元素反应击杀+100分，收集元素球+30分。

【道具】每关选一张卡，按元素战技键使用，效果同对应特殊球。另有一张净化：深渊球恢复可拾取(5秒)。

【敌人】每10秒生成一波，场上达上限后暂停(击杀后继续)。

【失败】倒计时归零或角色倒下则失败。
【重置】卡死时可按重置按钮重新开始本关。
`
  .trim()
  .replace(/\n/g, '\\n')

export const RuleBody = str(rawRuleBody)
